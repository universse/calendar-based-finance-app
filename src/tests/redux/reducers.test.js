import expect from 'expect'
import {} from 'reducers'

describe('Reducers', () => {
  describe('', () => {
    it('should properly ...', () => {
      let action = {
        type: ''
      }
      let actualState = {}
      expect(actualState).toEqual()
    })
  })
})
