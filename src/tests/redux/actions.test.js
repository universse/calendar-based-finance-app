import expect from 'expect'
import {} from 'actions'

describe('Actions', () => {
  it('should properly ...', () => {
    let expectedAction = {
      type: ''
    }
    let actualAction = {}
    expect(actualAction).toEqual(expectedAction)
  })
})
